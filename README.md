Uploader with Dropzone.js, Node.js and Express
1. Clone the repository
2. Ensure that `uploads/` is writeable
3. Run `npm install`
4. Run `bower install`
5. Run the app with `node index.js`
6. The app will run on port 8080
